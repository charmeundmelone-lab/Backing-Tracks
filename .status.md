- Hauptziel / letzter Stand: PLAN-show-automatik.md **komplett umgesetzt** (Teil 1-4, live vom User bestätigt: "funktioniert perfekt, so wie es soll"). Migration v19→v20, Mikrofon-Aufnahme im Song-Editor (Aufnahmequalität explizit gefixt), AudioEngine-Notiz-Pfad hart rechts, Pause-/Ansage-Ablauf bei Auto-Advance mit Live-Abbruch, "Frei spielen"-Freeze, Mikrofon-Icon in der Setlist, GrillMe-Nachtrag Ziffernblock-Zeit-Picker (MM:SS, max 60 Min., ausgelöst durch Nothing-OS-Timer-Screenshot) + Warnton (additive Glocken-Synthese, letztes Drittel der Pause gedeckelt bei 5 Min.), Diagnose-Tool "Automatik-Check". Letzter Commit `c065deb`, CI grün (Build #375).
- Betroffene Dateipfade: `data/Song.kt`, `data/AppDatabase.kt`, `data/SongDao.kt`, `audio/VoiceNoteRecorder.kt` (neu), `audio/AudioEngine.kt`, `audio/WavSynth.kt`, `audio/AutomatikCheck.kt` (neu), `ui/PlayerViewModel.kt`, `ui/GigViewModel.kt`, `ui/MainScreen.kt`, `ui/GigManagementScreen.kt`, `AndroidManifest.xml`, `PLAN-show-automatik.md`.
- Nächster Schritt: User-Feedback zum Lyrics-Teleprompter (noch NICHT umgesetzt, nur Machbarkeit bestätigt) — (1) Overlay soll sich automatisch schließen, wenn der Song zu Ende ist (Hook in den bestehenden 200ms-Poll, der auch Auto-Stop/Auto-Advance erkennt → zusätzlich `vm.closeLyrics()`), (2) der normale `GlobalPlayer` (Seekbar/Play-Pause-Stop/Loop/Automatik-Countdown) soll unten im Lyrics-Fenster mit angezeigt werden — aufwendiger, da zusätzliche Zustände/Callbacks (nextSong, Loop-State, Automatik-Status) in `LyricsOverlay.kt` durchgereicht werden müssen, aktuell nur in `MainScreen` vorhanden.
