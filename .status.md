- Hauptziel / letzter Stand: ✅ Archiv-Songzeile auf das Setlist-Design nachgezogen (dezente graue Nummer 14sp statt großer gelber, Songname 20sp Bold, aktiver Song mit 3dp Volt-Akzentleiste + gelbem Titel) UND Lesbarkeit von Tonart/Kapo verbessert (GigCool 0xFF9FB2C4 → 0xFFC2D6EA, Kapo bekommt dieselbe helle Farbe statt Grau, beide SemiBold, Meta-Zeile 11/12sp → 13sp) — gilt für Archiv und Setlist inkl. Sortier-Modus. Vom User live bestätigt ("das sieht super aus!"). Commit `9197839` auf main, CI Build #336 grün, APK an den User geschickt. Nichts uncommitted.
- Betroffene Dateipfade: app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (ArchivSongRow, neue Farbkonstante `Cool`, neuer Helper `archivMetaLine(song, alpha)` — alpha wird in jede Farbe gerechnet, KEIN graphicsLayer wegen Scroll-Performance), app/src/main/java/de/livegigplayer/pro/ui/GigManagementScreen.kt (GigCool aufgehellt, neuer Helper `songMetaLine(key, capo, duration)` für SetSongRow + SetSongRowSortable), CLAUDE.md (zwei Sprint-Einträge: Archiv-Redesign + der bis dahin undokumentierte Capo-Speicher-Bug `31bbbc1`).
- Nächster Schritt: Kein offener Bug. Falls der User weiter an der Lesbarkeit feilen will: Farbe/Schriftgröße der Meta-Zeile sind jetzt an genau zwei Stellen zentral (`Cool` in MainScreen.kt, `GigCool` in GigManagementScreen.kt — gleicher Wert, bewusst getrennte Paletten pro Datei). Weiterhin offen aus früheren Sessions: SetCard-Songliste ist noch nicht lazy (`forEach{}` statt LazyColumn, ein früherer Umbau crashte — siehe PRIO 1 in CLAUDE.md). Bei Session-Start zuerst `git branch` (muss `* main`) + CI-Status prüfen.
