- Hauptziel / letzter Stand: ✅ Tempo-Filter im Archiv gebaut (GrillMe-Interview vollständig durchgeführt → `PLAN-tempo-filter.md`). Room v18→19 (`Song.tempoTag`, manuell, NICHT aus BPM abgeleitet). Chips "Langsam · Mittel · Schnell" erscheinen sofort mit der Suche (kein zweites Aufklappen), genau einer aktiv wählbar, sterben mit der Suche (Filter reset beim Schließen), ungetaggte Songs werden bei aktivem Filter ausgeblendet. Tag wird nur im Song-Editor gesetzt (Bestand < 50 Songs, Editor-Weg reicht), im Archiv nirgends sichtbar. Commit `b35d790` auf main, CI Build #338 grün, APK an den User geschickt — **noch nicht live getestet**. Außerdem geklärt: der frühere PRIO-1-Punkt "SetCard-Songliste lazy machen" ist erledigt/verworfen — User hat das bestehende `forEach{}`-Scrollverhalten selbst getestet ("funktioniert wunderbar"), kein Umbau mehr nötig.
- Betroffene Dateipfade: app/src/main/java/de/livegigplayer/pro/data/Song.kt (neues Feld `tempoTag`), app/src/main/java/de/livegigplayer/pro/data/AppDatabase.kt (version=19, MIGRATION_18_19), app/src/main/java/de/livegigplayer/pro/ui/PlayerViewModel.kt (`_tempoFilter`/`setTempoFilter`, `filteredSongs` kombiniert jetzt 3 Flows, `saveSongEdits` mit `tempoTag`-Parameter), app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (SearchBar mit Chip-Reihe, SongEditorSheet mit Tempo-Chips, Aufrufstellen angepasst), PLAN-tempo-filter.md (neu), CLAUDE.md (Sprint-Eintrag + Datenmodell-Tabelle v19 + PRIO-1-Liste aktualisiert).
- Nächster Schritt: Live-Test des Tempo-Filters am Gerät (siehe PRIO 1 in CLAUDE.md — Checkliste: Chips erscheinen mit Suche, Filter/Toggle funktioniert, ungetaggte Songs verschwinden korrekt, Suche schließen setzt Filter zurück, Tag im Editor bleibt nach Speichern erhalten). Bei Session-Start zuerst `git branch` (muss `* main`) + CI-Status prüfen.
