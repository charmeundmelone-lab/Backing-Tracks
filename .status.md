- Hauptziel / letzter Stand: PLANUNGS-SESSION (2026-07-26, kein Code). PDF-Import direkt in der App per GrillMe komplett durchgeplant, **Weg A / Phase 1 entschieden**: erst PDF-Umbrüche 1:1 aus dem Textlayer übernehmen (keine eigene Umbruch-Regel), User testet am echten Song, Umbruch-Regel erst bei Bedarf aus realem Beispiel ableiten (Weg B, zurückgestellt). Geklärt: Kernschmerz = Medienbruch weg; Quelle Ultimate Guitar = echter Textlayer, kein OCR; offline mit PdfBox-Android voll machbar; Ablauf = PDF wählen → Text direkt ins Lyrics-Feld, User speichert selbst (kein Auto-Save). Alle Details in CLAUDE.md unter "🆕 Geplante Features → PDF-zu-Lyrics → ✅ ENTSCHEIDUNG (2026-07-26)". Kein Code offen, nichts uncommitted, main = origin/main.
- Betroffene Dateipfade (für die Umsetzungs-Session, noch NICHTS davon geändert): app/build.gradle.kts (PdfBox-Android-Dependency `com.tom-roush:pdfbox-android`), app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (SongEditorSheet → SAF-PDF-Picker + Import-Button, Ergebnis ins Lyrics-Feld), evtl. neue Hilfsklasse für Akkord-Filter-Regex (Kotlin-Port). Referenz-Skript existiert NICHT mehr im Repo/Scratchpad — die Filter-Logik ist in CLAUDE.md dokumentiert (Zeile = Akkordzeile bei ≥80% Akkord-Tokens gegen Regex `^[A-G](#|b)?(m|maj|min|sus|dim|aug|add)?\d{0,2}(/[A-G](#|b)?)?$` oder Taktstriche; `[Section]`-Labels behalten).
- Nächster Schritt (im FRISCHEN Chatfenster, so vom User gewünscht): Phase 1 umsetzen — PdfBox-Android einbinden, Akkord-Filter nach Kotlin portieren, SAF-Picker + Import-Button im SongEditorSheet, extrahierten+gefilterten Text (mit PDF-Umbrüchen 1:1) ins Lyrics-Feld schreiben, KEIN Auto-Save. Edge-Case beachten: belegtes Lyrics-Feld nicht still überschreiben. Danach an echtem UG-Song testen. Bei Session-Start zuerst `git branch` (muss `* main`) + CI-Status prüfen wie in CLAUDE.md vorgeschrieben.
