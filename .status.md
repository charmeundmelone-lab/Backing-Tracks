- Hauptziel / letzter Stand: ✅ Archiv-Scroll-Performance behoben + Set-Songliste-Scroll-Bug behoben + CI auf Release-Build umgestellt — vom User live bestätigt ("es läuft fantastisch!"). Drei Scroll-Ursachen nacheinander gefixt: (1) LazyColumn ohne key/contentType (db3914b), (2) gedimmte Zeilen legten pro Zeile einen graphicsLayer an → Alpha jetzt direkt in die Farben gerechnet (167ce5f), (3) "wird besser je öfter man scrollt" = Debug-Build-JIT-Warmup → CI baut jetzt Release (debuggable=false, ART-AOT) statt Debug (8cdcb57). Nebenbei: Set-Songliste war nicht scrollbar (untere Songs unerreichbar), SetCard bekommt weight(1f)+verticalScroll (cb2c362). Alles auf main, Build #330 grün, apk-dist = LiveGigPlayer-release.apk. Nichts uncommitted.
- Betroffene Dateipfade: app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (ArchivSongRow: LazyColumn-keys, Dimmen ohne Layer), ui/GigManagementScreen.kt (SetCard: modifier-Param + weight(1f).verticalScroll um die Song-Liste), app/build.gradle.kts (release mit Debug-Key signiert), .github/workflows/android-build.yml (assembleRelease + publiziert LiveGigPlayer-release.apk), CLAUDE.md (Build-Setup-Fetch + Sprint-Eintrag).
- Nächster Schritt: Kein offener Bug. WICHTIG: CI baut jetzt RELEASE — Fetch ist `git show origin/apk-dist:LiveGigPlayer-release.apk`. Optionale Politur (nur bei Wunsch): (a) Baseline-Profile für erste-Scroll-Kälte, (b) SetCard-Songs von forEach auf echtes LazyColumn (weniger dringend nach Release-Wechsel), (c) im Sortier-Modus Drag-Handle vs. verticalScroll sauber trennen, falls ein Set länger als der Bildschirm wird. Bei Session-Start zuerst `git branch` (muss `* main`) + CI-Status prüfen.
