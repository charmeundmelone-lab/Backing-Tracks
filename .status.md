- Hauptziel: Scroll-Performance in Archiv & Gig-Verwaltung war "noch nie sehr gut" (User-Report, unabhängig von der vorherigen Ausgrau-Feature-Session). Chirurgische Diagnose vor Umsetzung + User-Go abgewartet. ROOT CAUSE des eigentlichen Symptoms (ruckeliges Scrollen + rätselhaftes "★ Song → nächster"-Popup ohne Berührung) gefunden und behoben: detectHorizontalDragGestures im Archiv akkumulierte horizontale Bewegung unabhängig von vertikaler → schnelles Wischen löste versehentlich Swipe-Aktionen aus UND kämpfte um Touch-Events mit dem Scrollen. Fix: orientierungssensitive Custom-Geste (Achsen-Dominanz-Check vor Aktivierung). Live getestet, User-Feedback: "schon wesentlich besser". User hat weitere Politur bewusst zurückgestellt (kein Bug mehr offen, nur Wunsch nach noch mehr Feinschliff) — SIEHE NÄCHSTER SCHRITT.
- Dateipfade: app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (ArchivSongRow — neuer Gesture-Handler ~Zeile 1000, Imports ~Zeile 27-28/114-116); app/src/main/java/de/livegigplayer/pro/ui/GigManagementScreen.kt (SetSongRow — graphicsLayer+ModulateAlpha statt .alpha(), ~Zeile 859; SetCard-Songliste ist NACH EINEM CRASH wieder auf forEach{} statt LazyColumn zurückgesetzt, ~Zeile 667 — bewusst NICHT erneut versucht in dieser Session). Alles auf main, CI grün (Commit 356f903, aktueller HEAD). APK auf apk-dist.
- Nächster Schritt: SetCard-Songliste ist weiterhin NICHT lazy (forEach{} statt LazyColumn) — das ist der verbleibende Performance-Hebel, den der User zurückgestellt hat. Ein direkter LazyColumn-Umbau CRASHTE beim Set-Öffnen (vermutlich verschachtelte LazyColumn ohne Höhenbegrenzung in einer nicht-scrollenden Column innerhalb der äußeren Sets-LazyColumn) — VOR einem erneuten Versuch das Höhen-Constraint-Problem sauber lösen (z.B. heightIn(max=...) auf innerer LazyColumn, oder Sets+Songs als ein einziger flacher Item-Stream statt verschachtelter Listen), und den Fix isoliert verifizieren (kein Crash!) bevor er an den User geht. WICHTIG generell: vor jeder APK-Auslieferung CI-Status aktiv per mcp__github__actions_list/get_job_logs prüfen (nicht nur apk-dist fetchen) — in dieser Session wurde dem User zweimal versehentlich ein alter/kaputter Build geschickt, weil CI-Fehler nicht sofort bemerkt wurden. Volle Details siehe CLAUDE.md, Abschnitt "Scroll-Performance Archiv & Gig-Verwaltung (2026-07-25)".
