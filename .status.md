- Hauptziel / letzter Stand: GrillMe-Interview abgeschlossen (kein Code angefasst) — Ansage/Pause soll auch schnell DIREKT IM SET einstellbar sein, bewusst verbunden mit TODO-Punkt 8 (Player im Lyrics-Fenster), beides in einem Rutsch umsetzen. Dabei ein Logikfehler im ursprünglichen Plan gefunden und korrigiert: ein Icon nur im Player hätte laufende Musik beim Songwechsel abgewürgt (selectSong()→AudioEngine.load()→releaseCurrent()). Fix: Icon in der Set-Zeile wird primärer, engine-unabhängiger Weg; Player-Icon bleibt sekundär für den aktuell angewählten Song. Voller Plan in `PLAN-ansage-im-set.md`, Commit `ea8c5a8`, CI-Build für diesen reinen Doku-Commit noch nicht geprüft (kein App-Code geändert).
- Betroffene Dateipfade (für die anstehende Umsetzung): `PLAN-ansage-im-set.md` (neu, Spezifikation), `ui/GigManagementScreen.kt` (SetSongRow: Mic-Icon → IconButton), `ui/MainScreen.kt` (neues Mini-Sheet, Player-Icon in PlayerInfoBar/GlobalPlayer), `ui/LyricsOverlay.kt` (eingebetteter Player, TODO 8), `CLAUDE.md`.
- Nächster Schritt: Umsetzung von `PLAN-ansage-im-set.md` beginnen — erst prüfen, ob sich `VoiceNoteRow`+Aufnahme-Logik aus `SongEditorSheet` in eine wiederverwendbare Composable lösen lässt, dann Row-Icon in `SetSongRow` bauen (kleine Commits, nach jedem Schritt CI prüfen). Zentraler Regressionstest beim Live-Test: Song A läuft, Row-Icon von Song B antippen → A darf nicht stoppen.
