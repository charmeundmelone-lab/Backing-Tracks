- Hauptziel / letzter Stand: Vier Änderungen gebaut, alle CI-grün (Build #344, Commit `6efcc6a`), alle noch UNGETESTET: (1) Teleprompter — Lesepunkt auf 33 % Bildschirmhöhe statt oberem Rand, fester Lichtkegel-Verlauf, Volt-Balken an der aktiven Zeile (GrillMe-geplant); (2) Archiv-Mehrfachauswahl ging beim Set-Hinzufügen über Tempo-Filter hinweg verloren — gefixt; (3+4) "Songs hinzufügen" öffnet jetzt den Archiv-Tab als einzige Quelle der Wahrheit, `AddSongsToSetDialog` ersatzlos entfernt (~160 Zeilen Doppel-Logik). Der User hat die Vorversion im echten Gig eingesetzt: lief einwandfrei. NÄCHSTES THEMA laut User: **USB-Multitrack (Feedback-Sync, EP 0x81)** — er wollte dafür in einem frischen Chatfenster weitermachen. Nichts uncommitted, main = origin/main.
- Betroffene Dateipfade: app/src/main/java/de/livegigplayer/pro/ui/LyricsOverlay.kt (READPOINT_FRACTION/CONE_*-Konstanten, `leadPadTop`/`leadPadBottom`, Gradient-Overlay, `activeLineIdx`), app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (`addSongsTarget` in MainScreen, ArchivTab-Hinzufügen-Modus mit Banner/`AddToSetBar`/BackHandler, `isInTargetSet` in ArchivSongRow, `orderedIds` aus `allSongs`), app/src/main/java/de/livegigplayer/pro/ui/GigManagementScreen.kt (`onRequestAddSongs` durchgereicht bis SetCard, AddSongsToSetDialog gelöscht), CLAUDE.md (zwei Sprint-Einträge + Gotcha 12 aktualisiert).
- Nächster Schritt: Multitrack in neuer Session starten — konkreter technischer Punkt laut CLAUDE.md-TODO: **Feedback-Sync über Endpoint 0x81 (IN)** auslesen und Samples pro Intervall nachführen, um die Rest-Glitches und das Anlauf-Knacken zu beseitigen (KK1 Detach + KK2 isochroner Ton sind bereits am CQ20B bestanden, Code in `cpp/usb_tone.c` + `UsbIsoToneTester.kt`). Parallel offen zum Testen: die vier ungetesteten Änderungen oben (Teleprompter-Lesepunkt, Hinzufügen-über-Archiv). Bei Session-Start `git branch` (muss `* main`) + CI-Status prüfen.
