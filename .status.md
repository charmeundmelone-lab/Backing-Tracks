- Hauptziel / letzter Stand: ✅ Set-Umschalten (PLAN-set-umschalten.md, Schritte 1–4) VOLLSTÄNDIG umgesetzt & live bestätigt (User: "funktioniert fantastisch"). Griff-Button unter dem Gig-Header zeigt aktives Set + Fortschritt "X/Y gespielt", öffnet SetSwitcherSheet (Umschalten per Tap, Umbenennen, Löschen, Drag-Sortieren, "+ Neues Set", Auto-Übergang-Schalter). `switchToSet` unterbricht einen laufenden Song NICHT (nur Queue wird umgebogen), armt bei Pause/Idle sofort. Danach 2 Politur-Fixes aus direktem User-Feedback: Griff auffälliger gestaltet (Rahmen + "Wechseln"-Text statt nur Pfeil), doppelte Set-Name/Nummer/Songzahl-Anzeige aus dem SetCard-Header entfernt (stand schon im Griff). Commits 00ef563 → 74e5ee3 → 90115b6, alle CI grün (Build #325), main = origin/main, nichts uncommitted. Details in CLAUDE.md-Sprint "Set-Umschalten DONE".
- Betroffene Dateipfade: app/src/main/java/de/livegigplayer/pro/data/GigEntity.kt (lastActiveSetId, autoAdvanceSets), data/AppDatabase.kt (v17→v18, MIGRATION_17_18), data/GigDao.kt (setLastActiveSetId, setAutoAdvanceSets), data/SetDao.kt (getSetProgress, getSetsForGigOnce), data/SetProgress.kt (NEU), ui/GigViewModel.kt (switchToSet, installSetCompletedCallback, setAutoAdvance, setProgress), ui/GigManagementScreen.kt (GigDetailView-Griff, SetSwitcherSheet/SetSwitcherRow NEU, SetCard-Header entschlackt).
- Nächster Schritt: Kein offener Bug — Feature ist fertig und vom User am Gerät bestätigt. Nur noch die Test-Checkliste aus PLAN-set-umschalten.md bei Gelegenheit vollständig durchgehen (v.a. Punkt 6: Set-Persistenz nach App-Neustart, Punkt 8: Verhalten bei aktivem isLocked). Bei Session-Start zuerst `git branch` (muss `* main`) + CI-Status prüfen.
