- Hauptziel / letzter Stand: User-Report "einzelne Songs verschwinden aus Gig-Sets, Sunny Afternoon spielt nicht mehr" untersucht — bewusst kein Blind-Fix, erst Diagnose-Tool gebaut und ausgewertet. Ergebnis vom Gerät: 0 defekte Verknüpfungen, 0 Duplikate unter allen 38 Songs — die Verdachtstheorie (Reimport erzeugt bei geänderter SAF-URI eine neue Song-ID ohne Set-Zuordnung) ist damit widerlegt, die Set-Zuordnung war die ganze Zeit intakt. Tatsächliche Ursache: die WAV-Datei von "Sunny Afternoon" war beschädigt (kein App-Bug, vermutlich vom 48-kHz-Konvertier-Skript verursacht) — User hat die Originaldatei vom Laptop neu reinkopiert und reimportiert, funktioniert wieder. Plausible Erklärung für den ursprünglichen "Song fehlt"-Eindruck: `AudioEngine` hat keinen Fehler-Listener, ein Ladefehler zeigt sich als stilles Nichtstun statt Fehlermeldung — ein entsprechender Fix wurde angeboten und vom User als "nicht mehr nötig" abgelehnt. Thema abgeschlossen. CI Build #363 grün, Commit `31e608c`.
- Betroffene Dateipfade: audio/SongLinkCheck.kt (neu — Diagnose-Bericht: defekte Dateiverknüpfungen + Duplikat-Erkennung), audio/WavFormatCheck.kt (wavFilesOf private → internal, wiederverwendet von SongLinkCheck), data/SetDao.kt (neue Query getAllCrossRefMembershipOnce), data/SetWithSongs.kt (neue Datenklasse CrossRefMembership), ui/GigViewModel.kt (Wrapper allCrossRefMembershipOnce), ui/MainScreen.kt (Menüpunkt "Song-Verknüpfungen prüfen", SongLinkCheckDialog), CLAUDE.md.
- Nächster Schritt: Kein offener Punkt aus diesem Thema. Laut CLAUDE.md-TODO-Liste stehen weiterhin zwei Wege zur Wahl, der User hat sich noch nicht entschieden: (a) KLEIN — sobald er einen Song testweise als Stems aus Studio One exportiert und die Dateinamen schickt, `SongScanner` so umbauen, dass der Rollenname IM Dateinamen erkannt wird (heute nur exakt drums/bass/keys/vocals/click/cue.wav). (b) GROSS — USB-Multitrack: Feedback-Sync über EP 0x81 in cpp/usb_tone.c, behebt "Töne kamen nach und nach dazu" + Anlauf-Knacken. Kein Gradle-Build in der Sandbox möglich (Google-Maven 403) → Test-Loop bleibt: Push auf main → CI legt APK auf apk-dist → User testet am Gerät.
