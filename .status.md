- Hauptziel: Scroll-Performance-Session (2026-07-25) ist ABGESCHLOSSEN und vom User live getestet ("wesentlich besser") — CI grün auf Commit 356f903, Übergabe in CLAUDE.md dokumentiert (Commit 81ef7d4). Danach kurzer Exkurs: PDF-zu-Lyrics-Konvertierung für einen Song (Whitesnake "Here I Go Again") extern per Python-Skript erledigt (Content-Filter blockte direkte Songtext-Ausgabe im Chat — Songtext lief nur durchs Skript, Ergebnis als Datei an den User). Danach Machbarkeits-Frage besprochen: PDF-Import DIREKT in die App integrieren — als neues Feature in CLAUDE.md unter "🆕 Geplante Features" dokumentiert, NOCH NICHT umgesetzt, nur Konzept/Machbarkeit geklärt. Kein Code offen, nichts uncommitted.
- Dateipfade: Für die neue PDF-Import-Feature-Idee (falls als nächstes umgesetzt): kein Code existiert noch. Referenz-Skript liegt NUR im Scratchpad (nicht im Repo!) unter /tmp/claude-0/.../scratchpad/pdf_to_lyrics.py — falls die Logik gebraucht wird, mit User klären ob als Referenz erneut angefragt werden soll oder ob direkt in Kotlin neu geschrieben wird. Scroll-Performance-Fixes (abgeschlossen) betrafen: app/src/main/java/de/livegigplayer/pro/ui/MainScreen.kt (ArchivSongRow) und GigManagementScreen.kt (SetSongRow, SetCard) — siehe CLAUDE.md Abschnitt "Scroll-Performance Archiv & Gig-Verwaltung (2026-07-25)" für Details.
- Nächster Schritt: Mit dem User klären, ob jetzt das PDF-Import-Feature umgesetzt werden soll (siehe CLAUDE.md "🆕 Geplante Features" → PDF-zu-Lyrics-Punkt: PdfBox-Android als Dependency, Akkord-Filter-Regex nach Kotlin portieren, SAF-Picker im Song-Editor, Ergebnis ins Lyrics-Feld einfügen ohne Auto-Speichern) — oder ob eines der anderen offenen Punkte Vorrang hat: (a) SetCard-Songliste ist weiterhin nicht lazy (forEach statt LazyColumn, User hat das bewusst zurückgestellt, siehe PRIO 1 in CLAUDE.md TODOs — Crash-Risiko beim letzten Versuch, siehe dortige Warnung), (b) Lyrics-Teleprompter Live-Test (Sprint 5.52, ungetestet), (c) Bluetooth-Fußschalter oder USB-Multitrack (beide größere, noch nicht begonnene Features). Bei Session-Start zuerst `git branch` (muss `* main` zeigen) und CI-Status prüfen wie in CLAUDE.md "SESSION-START" vorgeschrieben.
